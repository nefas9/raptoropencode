---
description: Scan a repository with Semgrep and CodeQL (alias for /scan)
---

# /raptor-scan - alias for /scan

This command is retained as a back-compat alias. The canonical
slash-command is `/scan` — please use that going forward.

See: scan.md
